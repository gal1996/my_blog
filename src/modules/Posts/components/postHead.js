import React from 'react';

const postHead = (props) => (
    <div id='postHead'>
        <div id='postTitle'>
            <h1>{this.props.title}</h1>
        </div>
        <div id='postDate'>
            <p>{this.props.date}</p>
        </div>
    </div>
);

export default postHead;
